const styles = (theme) => ({
  root: {
    width: "100%",
    maxWidth: 360,
    backgroundColor: "beige",
  },
  noChatroom: {
    backgroundColor: "beige",
    maxWidth: 360,
    minHeight: 42,
    display: "flex",
    justifyContent: "center",
    alignItems: "center",
    fontSize: 14,
  },
});

export default styles;
